[
  {
    "source_url": "https://hackerone.com/reports/3321406",
    "source_platform": "hackerone",
    "vuln_class": "xss",
    "vuln_subtype": "self-xss-elevated-via-cookie-tossing-and-csrf-prediction",
    "cwe_id": "CWE-79",
    "affected_feature_type": "auth_endpoint",
    "affected_stack_hints": [],
    "behavioral_signal": "A reflected/self-XSS that initially looks low-impact becomes a one-click takeover when chained with the ability to set cookies on a sibling subdomain (cookie tossing) and a predictable or reusable anti-CSRF token, ultimately auto-approving a privileged action like temporary auth or session elevation.",
    "detection_approach": "Map every authenticated state-change endpoint and identify which anti-CSRF tokens it accepts. For each token, determine generation source (HMAC of session, random, derived from cookie) and whether the application also reads any value from a cookie that is settable from a sibling subdomain. Identify any XSS sink \u2014 even self-XSS \u2014 that runs in the application origin. Sketch the chain: attacker primes cookies on a sibling subdomain (subdomain takeover, open subdomain, or attacker-controlled subdomain), uses self-XSS or cookie tossing to inject a value the server later treats as authenticated context, and replays a state-change request whose CSRF token was either predictable or set by the attacker. Look specifically at workflows that auto-approve when the token matches.",
    "oob_required": false,
    "preconditions": [
      "Application has at least one XSS sink, even if only self-XSS",
      "Cookies set on a sibling subdomain are honored on the privileged subdomain",
      "Anti-CSRF token is predictable or derived from a value the attacker can influence"
    ],
    "root_cause_pattern": "Three weak controls \u2014 a self-XSS sink, broad cookie-domain scoping, and a CSRF token whose entropy or binding is insufficient \u2014 were each judged independently low-severity, so none was hardened. Together they form a one-click takeover.",
    "chain_potential": "high",
    "chain_targets": [
      "auth_bypass",
      "csrf",
      "subdomain_takeover"
    ],
    "chain_reasoning": "This pattern is a textbook chain: XSS provides script execution, cookie scoping provides cross-subdomain influence, CSRF prediction completes the privileged action. Subdomain takeover frequently supplies the cookie-toss origin.",
    "severity": "high",
    "payout_usd": null,
    "is_novel": false,
    "novel_description": null,
    "extraction_confidence": 0.7,
    "skipped": false,
    "skip_reason": null
  }
]
